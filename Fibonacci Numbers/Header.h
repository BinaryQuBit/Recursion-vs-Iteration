#ifndef HEADER_H_INCLUDED
#define HEADER_H_INCLUDED

// Two functions in header used for recursion and iteration
int recursion(int num);
int iteration(int num[], int size);


#endif // HEADER_H_INCLUDED
